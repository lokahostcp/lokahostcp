#!/bin/bash
# info: change system web terminal backend port
# options: PORT
#
# example: v-change-sys-web-terminal-port 5678
#
# This function for changing the system's web terminal backend port in NGINX configuration.

#----------------------------------------------------------#
#                Variables & Functions                     #
#----------------------------------------------------------#

# Argument definition
PORT=$1
NGINX_CONFIG="$LOKAHOSTCP/nginx/conf/nginx.conf"

# Includes
# shellcheck source=/etc/lokahostcp/lokahostcp.conf
source /etc/lokahostcp/lokahostcp.conf
# shellcheck source=/usr/local/lokahostcp/func/main.sh
source $LOKAHOSTCP/func/main.sh
# load config file
source_conf "$LOKAHOSTCP/conf/lokahostcp.conf"

# Functions
is_port_valid() {
	# Check if PORT is numeric
	if [[ ! "$PORT" =~ ^[0-9]+$ ]]; then
		echo "Port should contains a numeric value only!"
		log_event "$E_INVALID" "$ARGUMENTS"
		exit "$E_INVALID"
	fi

	# Check if PORT is already used
	BUSY_PORT=$(lsof -i:"$PORT")
	if [ -n "$BUSY_PORT" ] && [ "$PORT" != "$BACKEND_PORT" ]; then
		echo "Port is already used by Lokahostcp, please set another one!"
		log_event "$E_INUSE" "$ARGUMENTS"
		exit "$E_INUSE"
	fi
}

#----------------------------------------------------------#
#                    Verifications                         #
#----------------------------------------------------------#

check_args '1' "$#" 'PORT'
is_port_valid

# Perform verification if read-only mode is enabled
check_lokahostcp_demo_mode

#----------------------------------------------------------#
#                       Action                             #
#----------------------------------------------------------#

# Get original port
ORIGINAL_PORT=$(cat ${NGINX_CONFIG} | grep -m1 "proxy_pass http://localhost:" | sed 's/[^0-9]*//g')

# Check if port is different to nginx.conf
if [ "$ORIGINAL_PORT" = "$PORT" ]; then
	# Nothing to do, exit
	exit
else
	# Set new port in config via v-change-sys-config-value
	$BIN/v-change-sys-config-value "WEB_TERMINAL_PORT" "$PORT"
	# Replace port in config files.
	sed -i "s/\(proxy_pass http:\/\/localhost:\)[0-9][0-9]*\([^0-9]*\;$\)/\1$PORT\2/" ${NGINX_CONFIG}

	# Check if the web terminal backend is running
	if [[ $(ps -eaf | grep -i lokahostcp/web-terminal | sed '/^$/d' | wc -l) -gt 1 ]]; then
		$BIN/v-restart-service lokahostcp-web-terminal
	fi

	# Check if Lokahostcp is running
	if [[ $(ps -eaf | grep -i lokahostcp | sed '/^$/d' | wc -l) -gt 1 ]]; then
		$BIN/v-restart-service lokahostcp
	fi
fi

#----------------------------------------------------------#
#                       Lokahostcp                             #
#----------------------------------------------------------#

# Logging
$BIN/v-log-action "system" "Warning" "System" "Lokahostcp Control Panel web terminal port changed (New Value: $PORT, Old Value: $ORIGINAL_PORT)."
log_event "$OK" "$ARGUMENTS"

exit
