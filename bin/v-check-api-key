#!/bin/bash
# info: check api key
# options: KEY [IP]
#
# example: v-check-api-key random_key 127.0.0.1
#
# This function checks a key file in $LOKAHOST/data/keys/

#----------------------------------------------------------#
#                Variables & Functions                     #
#----------------------------------------------------------#

# Includes
# shellcheck source=/etc/lokahost/lokahost.conf
source /etc/lokahost/lokahost.conf
# shellcheck source=/usr/local/lokahost/func/main.sh
source $LOKAHOST/func/main.sh
# load config file
source_conf "$LOKAHOST/conf/lokahost.conf"

new_timestamp

abort_missmatch() {
	echo "Error: key missmatch"
	echo "$date $time api $ip failed to login" >> $LOKAHOST/log/auth.log
	exit "$E_PASSWORD"
}

ip=${2-127.0.0.1}
time_n_date=$(date +'%T %F')
time=$(echo "$time_n_date" | cut -f 1 -d \ )
date=$(echo "$time_n_date" | cut -f 2 -d \ )

# Perform verification if read-only mode is enabled
check_lokahost_demo_mode

#----------------------------------------------------------#
#                       Action                             #
#----------------------------------------------------------#

key="$(basename "$1")"

# Exit if Key is unset or to short
[[ -z $key || ${#key} -lt 16 ]] && abort_missmatch

# Key file must exist
maybe_key_path="$(readlink -e "${LOKAHOST}/data/keys/${key}")"
[[ -z $maybe_key_path ]] && abort_missmatch

# Key file cannot be the key store
[[ $maybe_key_path == "${LOKAHOST}/data/keys" ]] && abort_missmatch

# Key file must be in the key store
[[ $maybe_key_path == "${LOKAHOST}/data/keys/"* ]] || abort_missmatch

#----------------------------------------------------------#
#                       Lokahost                             #
#----------------------------------------------------------#

echo "$date $time api $ip successfully launched" >> $LOKAHOST/log/auth.log

exit
