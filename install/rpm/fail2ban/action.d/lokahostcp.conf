# Fail2Ban configuration file for lokahostcp

[Definition]

actionstart = /usr/local/lokahostcp/bin/v-add-firewall-chain <name>
actionstop = /usr/local/lokahostcp/bin/v-delete-firewall-chain <name>
actioncheck = iptables -n -L INPUT | grep -q 'fail2ban-<name>[ \t]'
actionban = /usr/local/lokahostcp/bin/v-add-firewall-ban <ip> <name>
actionunban = /usr/local/lokahostcp/bin/v-delete-firewall-ban <ip> <name>