# Created by lokahostcp installer
Defaults env_keep="VESTA"
Defaults env_keep+="LOKAHOSTCP"
Defaults:admin !syslog
Defaults:admin !requiretty
Defaults:root !requiretty

# sudo is limited to lokahostcp scripts
admin   ALL=NOPASSWD:/usr/local/vesta/bin/*
admin   ALL=NOPASSWD:/usr/local/lokahostcp/bin/*
