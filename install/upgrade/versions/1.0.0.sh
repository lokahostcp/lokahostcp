#!/bin/bash
# Lokahostcp Control Panel 1.0.0
#
# Initial release. No migration steps are required.
#
# Upgrade scripts inherited from the upstream project were removed at the
# 1.0.0 version reset: func/upgrade.sh sources every version script whose
# version is greater than the installed VERSION, so retaining the upstream
# 1.0.1-1.9.0 scripts would have run all of them against a fresh 1.0.0
# install.
